apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello
spec:
  template:
    spec:
      containers:
      - name: hello
        resources:
          requests:
            cpu: "25m"
          limits:
            cpu: "2"
